<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Translator" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Disable window enter/exit animations -->
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Set window background to match Material3 surface color -->
        <item name="android:windowBackground">?attr/colorSurface</item>
        <!-- Ensure no window enter/exit transitions -->
        <item name="android:windowEnterTransition">@null</item>
        <item name="android:windowExitTransition">@null</item>
        <item name="android:windowSharedElementEnterTransition">@null</item>
        <item name="android:windowSharedElementExitTransition">@null</item>
    </style>

    <style name="Theme.Translator.CropImage" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>
</resources>