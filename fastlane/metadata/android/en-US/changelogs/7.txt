- Implement favorites; sort favorite languages first
- Implement API so other apps can request for translation (@dic1911)
- Add setting to enable File picker for OCR
- Make OCR only use 'from' language for detection (instead of all languages)
